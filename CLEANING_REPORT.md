# Cleaning Report

Changes made for public release:

- selected only the final/superseding analysis notebooks;
- cleared execution counts and notebook outputs;
- removed convenience `%pip install` cells;
- removed the local absolute project path from notebook code and markdown;
- standardized project configuration through `RAJSHAHI_PROJECT_ROOT`;
- moved helper modules into `src/`;
- standardized notebook metadata to Python 3.11;
- renamed notebooks into one numeric run order;
- extended the final summary notebook to collect the already reported road outputs;
- added dependency, data, citation, validation, and GitHub release documentation.

The scientific thresholds and reported analytical logic were not changed.

The missing original 8-band feature-stack-generation code was not recreated from assumptions.
